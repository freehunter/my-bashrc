If you add these functions to ~/.bashrc then run source ~/.bashrc, you can

1. Run one command to start both Postgres and Rails (startserver)

2. Run one command to push the new changes to Git (gitpush)
